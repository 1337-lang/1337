thing := 10
typed_thing : i32 = 1337
pi := 3.1415926535
text := "hello world!"
no_value : i64

basic_proto : fn ()
args_proto : fn (a: i32, b: i32)
advanced_proto : fn i64 (a: i32, b: i32)

basic := fn () {
	
}

basic_two := fn (x: i32) {
	abc := x
}

binop := 1 + 2
binop2 := 2 * 3 + 1
complicated_binop := 1 * 2 + 3 * 4

sum := fn i64 (x: i32, y: i32) {
	result := x + y
	result
}

main := fn (argc: i32, argv: []str) {
	number := 10
}
